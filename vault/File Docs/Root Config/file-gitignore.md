---
file: .gitignore
owner: כל הצוות (תשתית)
type: config
tags:
  - file-docs
  - git
---

# .gitignore

**נתיב:** `.gitignore`
**שייך ל:** תשתית משותפת

## מה הקובץ עושה
מגדיר אילו קבצים Git מתעלם מהם. כרגע מחריג: סודות סביבה (`.env`, `.env.local`, `.env.*.local`) וקבצי מערכת (`Thumbs.db`, `.DS_Store`).

## קבצים קשורים
- [[file-env]] — הסוד העיקרי שמוחרג כאן
- [[file-env-example]] — התבנית שכן עולה ל-Git
